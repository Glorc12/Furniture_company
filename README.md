# 🪑 Furniture Company Management System

Система управления продукцией мебельной компании с Flask + PostgreSQL + Vue.js

## 📋 Возможности

- ✅ Управление продукцией (создание, редактирование, удаление)
- ✅ Управление цехами производства
- ✅ Связь продуктов с цехами и временем производства
- ✅ REST API для всех операций
- ✅ Интерактивный веб-интерфейс

## 🛠️ Технологический стек

**Backend:**
- Python 3.8+
- Flask - веб-фреймворк
- Flask-SQLAlchemy - ORM
- Flask-CORS - поддержка CORS
- PostgreSQL - база данных

**Frontend:**
- HTML5
- CSS3
- JavaScript (ES6+)
- Fetch API

## 📁 Структура проекта

```
Furniture_company/
├── Furniture/
│   ├── models/              # SQLAlchemy модели
│   │   ├── __init__.py
│   │   ├── product.py       # Модели Product, ProductType, MaterialType
│   │   └── workshop.py      # Модели Workshop, ProductWorkshop
│   ├── routes/              # API маршруты
│   │   ├── __init__.py
│   │   ├── products.py      # Эндпоинты для продуктов
│   │   └── workshops.py     # Эндпоинты для цехов
│   ├── services/            # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── product_service.py
│   │   └── workshop_service.py
│   ├── frontend/            # Статические файлы
│   │   ├── index.html
│   │   ├── js/
│   │   │   ├── api.js       # Функции для API запросов
│   │   │   ├── ui.js        # Управление интерфейсом
│   │   │   └── app.js       # Инициализация приложения
│   │   └── css/
│   │       └── styles.css
│   ├── app.py               # Flask приложение
│   ├── config.py            # Конфигурация
│   ├── database.py          # Инициализация БД
│   └── requirements.txt     # Зависимости Python
├── furniture_company.sql    # SQL дамп БД
└── README.md               # Этот файл
```

## 🚀 Быстрый старт

### Требования

- Python 3.8+
- PostgreSQL 12+
- pip

### Установка

1. **Клонируй репозиторий**
```bash
git clone https://github.com/Glorc12/Furniture_company.git
cd Furniture_company/Furniture
```

2. **Создай виртуальное окружение**
```bash
python -m venv .venv
.\.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/Mac
```

3. **Установи зависимости**
```bash
pip install -r requirements.txt
```

4. **Настрой БД**

Создай базу данных PostgreSQL:
```sql
CREATE DATABASE furniture_company;
```

Обнови `config.py` с параметрами подключения:
```python
SQLALCHEMY_DATABASE_URI = 'postgresql://user:password@localhost:5432/furniture_company'
```

Загрузи данные из `furniture_company.sql`:
```bash
psql -U user -d furniture_company -f furniture_company.sql
```

5. **Запусти Flask сервер**
```bash
python app.py
```
Сервер запустится на `http://localhost:5000`

6. **Запусти фронтенд** (в другом терминале)
```bash
cd frontend
python -m http.server 8000
```
Откройте браузер: `http://localhost:8000`

## 📡 API Эндпоинты

### Продукты

- `GET /api/products` - Получить все продукты
- `GET /api/products/<id>` - Получить продукт по ID
- `POST /api/products` - Создать новый продукт
- `PUT /api/products/<id>` - Обновить продукт
- `DELETE /api/products/<id>` - Удалить продукт

### Цеха

- `GET /api/workshops` - Получить все цеха
- `GET /api/workshops/<id>` - Получить цех по ID
- `GET /api/workshops/product/<product_id>` - Получить цеха для продукта

## 🎨 Основные функции

### Управление продукцией

1. Просмотр списка всех продуктов в таблице
2. Редактирование продукта (артикул, наименование, тип, материал, цена)
3. Удаление продукта с подтверждением
4. Добавление нового продукта через форму

### Управление цехами

1. Просмотр всех цехов
2. Просмотр цехов, задействованных в производстве конкретного продукта
3. Отображение времени производства для каждого цеха

## ⚙️ Конфигурация

Основные параметры в `config.py`:

```python
# Подключение к БД
SQLALCHEMY_DATABASE_URI = 'postgresql://user:password@localhost:5432/furniture_company'

# Отключение трекинга модификаций
SQLALCHEMY_TRACK_MODIFICATIONS = False

# Режим debug
DEBUG = True
```

## 🐛 Решение проблем

### Ошибка подключения к Flask

**Проблема:** `net::ERR_CONNECTION_REFUSED` при запросе к API

**Решение:**
1. Убедись, что Flask запущен: `python app.py`
2. Проверь адрес в `api.js`: должен быть `http://localhost:5000/api`
3. Убедись, что фронтенд и бэкенд на разных портах (8000 и 5000)

### Ошибка БД

**Проблема:** `psycopg2.OperationalError: could not connect to server`

**Решение:**
1. Убедись, что PostgreSQL запущен
2. Проверь параметры подключения в `config.py`
3. Убедись, что база данных создана

### CORS ошибки

**Проблема:** Cross-Origin Request Blocked

**Решение:** CORS уже настроен в `app.py`, но если нужно, добавь больше origins:
```python
CORS(app, resources={r"/*": {"origins": "*"}})
```

## 📚 Модели данных

### Product
- `product_id` - Уникальный идентификатор
- `article_number` - Артикул (уникальный)
- `product_name` - Наименование продукта
- `product_type_id` - Тип продукта (FK)
- `material_type_id` - Тип материала (FK)
- `minimum_partner_price` - Минимальная цена партнёра

### Workshop
- `workshop_id` - Уникальный идентификатор
- `workshop_name` - Название цеха
- `workshop_type` - Тип цеха
- `staff_count` - Количество сотрудников

### ProductWorkshop
- `product_workshop_id` - Уникальный идентификатор
- `product_id` - ID продукта (FK)
- `workshop_id` - ID цеха (FK)
- `manufacturing_time_hours` - Время производства (часов)

## 👨‍💻 Разработчик

Glorc12 - [GitHub](https://github.com/Glorc12)

## 📝 Лицензия

MIT License

## 🤝 Поддержка

Если найдёшь ошибку или есть предложения — открой Issue на GitHub!
